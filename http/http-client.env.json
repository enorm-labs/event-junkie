{
    "local": {
        "importer-host": "http://localhost:8081",
        "bff-host": "http://localhost:8080"
    },
    "staging": {
        "importer-host": "http://localhost:18081",
        "//bff-host": "Carries /api because the BFF serves that prefix itself on a cluster (spring.webflux.base-path, set from bff.basePath in the chart) and does NOT serve it locally. Without it every request here matches the ingress's `/` rule instead, reaches the frontend, and comes back 200 with the SPA shell — see docs/ops/CLUSTER_ACCESS.md §6.",
        "bff-host": "https://staging.event-junkie.de/api"
    }
}

