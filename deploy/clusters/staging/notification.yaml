# GitHub deployments for staging — what is running here, and since when (#565).
#
# **There is deliberately no `github` commit-status Provider here (#567).** "Why is there no commit
# status?" is the obvious question and the answer is not: notification-controller's `parseRevision`
# needs a git hash, while a HelmRelease event reports the *chart version*. The mechanism that solves
# this — `originRevision`, derived from `org.opencontainers.image.revision` — exists only in
# **kustomize-controller**; helm-controller has no equivalent, so no annotation on the chart can fix
# it. See §4a.
---
# A deployment is per-environment and produces a timeline, which is the question worth answering:
# "what is on staging right now, and since when" has no single commit to ask about.
apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: github-dispatch
  namespace: flux-system
  annotations:
    kubernetes.io/description: >-
      Fires a repository_dispatch so .github/workflows/deployment-status.yml can record a
      GitHub deployment for this cluster.
spec:
  type: githubdispatch
  address: https://github.com/enorm-labs/event-junkie
  # THE ONLY CREDENTIAL THIS CLUSTER HOLDS FOR GITHUB'S SAKE, and worth being explicit about because
  # §4's argument rests on the cluster holding nothing pointing back at us.
  #
  # `repository_dispatch` needs **contents: write** — a strong scope, and the reason this token is
  # per-cluster rather than shared: revoking one must not take the other down, and "which cluster
  # wrote that" should stay answerable at the API level.
  #
  # Fine-grained, scoped to this repository, and **hand-made rather than SOPS-encrypted**
  # (docs/ops/SECRETS.md). It does not inherit the SOPS answer #416 gave the other secrets: encrypting a value into a PUBLIC repository publishes its ciphertext permanently, and
  # `contents: write` is the credential where that costs most — the same argument that kept the
  # Hetzner token hand-made.
  #
  # So NOTHING IN THIS REPOSITORY CREATES IT and no deploy will bring it. Typed once, out of band:
  #
  #   kubectl --context event-junkie-staging create secret generic github-dispatch \
  #     -n flux-system --from-literal=token=…
  #
  # Until it exists this Provider reconciles into a failed state and no dispatch is sent.
  secretRef:
    name: github-dispatch
---
apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Alert
metadata:
  name: github-dispatch
  namespace: flux-system
spec:
  providerRef:
    name: github-dispatch
  eventSeverity: info
  # Deliberately narrower than the commit-status Alert above, which also watches the OCIRepository.
  # A chart arriving in the registry is not a deployment — it is what a deployment later consumes —
  # and recording it as one would put two entries on the timeline for a single rollout.
  eventSources:
    - kind: HelmRelease
      name: event-junkie
      namespace: flux-system
  # These two are the workflow's whole input beyond the revision: `env` picks the GitHub environment
  # and `cluster` is recorded on the deployment. An unrecognised `env` fails the run rather than
  # defaulting, so a typo here is loud instead of filing staging under production.
  eventMetadata:
    env: staging
    cluster: staging
