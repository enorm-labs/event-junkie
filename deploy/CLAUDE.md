# CLAUDE.md

Guidance for anything under `deploy/` lives in @AGENTS.md, next to this file. Read it before changing the chart — it starts with the commands that are safe to
run here, and the ones that must never be run against a real cluster.
