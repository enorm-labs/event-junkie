{
  "$schema": "./node_modules/oxfmt/configuration_schema.json",
  "//": "schema.d.ts is generated by openapi-typescript and committed as the generator writes it, so a regeneration's diff is the API change and nothing else. Formatting it once turns every later regeneration into a whole-file rewrite.",
  "ignorePatterns": ["src/api/schema.d.ts"],
  "semi": false,
  "singleQuote": true
}
