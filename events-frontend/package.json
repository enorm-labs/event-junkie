{
  "name": "event-junkie",
  "version": "0.3.10",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "run-p type-check \"build-only {@}\" --",
    "preview": "vite preview",
    "test:unit": "vitest",
    "test:unit:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "//test:a11y": "The axe/WCAG sweep on its own, for iterating on markup without paying for the whole e2e suite. It also runs as part of `test:e2e` — this is a filter, not a second check, so there is nothing extra to wire into CI.",
    "test:a11y": "playwright test a11y.spec.ts",
    "build-only": "vite build",
    "//generate:api": "Version pinned EXACTLY, and deliberately still `npx` rather than a devDependency. openapi-typescript@7 declares `peer typescript@^5.x` and this project is on TypeScript 6, so installing it locally fails to resolve; npx runs it in isolation with its own TypeScript, which is why it works at all. Pinning the exact version closes the reproducibility half of the problem (an unpinned range can reshape the whole generated file), and the isolation means its TypeScript never enters our tree. Revisit when openapi-typescript widens the peer range. Requires a running BFF — see README.",
    "generate:api": "npx -y openapi-typescript@7.13.0 http://localhost:8080/v3/api-docs -o src/api/schema.d.ts",
    "generate:notices": "node scripts/generate-notices.mjs",
    "check:licenses": "node scripts/check-licenses.mjs",
    "type-check": "vue-tsc --build",
    "lint": "run-s lint:*",
    "lint:oxlint": "oxlint --fix",
    "lint:eslint": "eslint --fix --cache",
    "format": "oxfmt src"
  },
  "dependencies": {
    "@fontsource-variable/geist": "5.3.0",
    "@fontsource-variable/geist-mono": "5.3.0",
    "@fontsource-variable/rubik": "5.3.0",
    "@fullcalendar/vue3": "7.0.2",
    "@lucide/vue": "1.39.0",
    "class-variance-authority": "0.7.1",
    "clsx": "2.1.1",
    "openapi-fetch": "0.17.0",
    "reka-ui": "2.10.4",
    "tailwind-merge": "3.6.0",
    "tailwindcss": "4.3.3",
    "temporal-polyfill": "1.0.4",
    "tw-animate-css": "1.4.0",
    "vue": "3.5.42",
    "vue-i18n": "11.4.10",
    "vue-router": "5.3.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@intlify/unplugin-vue-i18n": "11.2.5",
    "@playwright/test": "1.62.1",
    "@tailwindcss/vite": "4.3.3",
    "@tsconfig/node24": "24.0.5",
    "@types/jsdom": "30.0.0",
    "@types/node": "26.4.1",
    "@vitejs/plugin-vue": "6.0.8",
    "@vitest/coverage-v8": "4.1.11",
    "@vitest/eslint-plugin": "1.6.27",
    "@vue/eslint-config-typescript": "14.9.0",
    "@vue/test-utils": "2.5.0",
    "@vue/tsconfig": "0.9.1",
    "eslint": "10.9.1",
    "eslint-config-prettier": "10.1.8",
    "eslint-plugin-oxlint": "1.81.0",
    "eslint-plugin-playwright": "2.11.0",
    "eslint-plugin-vue": "10.10.0",
    "eslint-plugin-vuejs-accessibility": "2.6.0",
    "jiti": "2.7.0",
    "jsdom": "30.0.1",
    "license-checker-rseidelsohn": "5.0.1",
    "npm-run-all2": "9.0.3",
    "oxfmt": "0.66.0",
    "oxlint": "1.81.0",
    "shadcn-vue": "2.8.2",
    "typescript": "6.0.3",
    "vite": "8.2.2",
    "vite-plugin-vue-devtools": "8.2.1",
    "vitest": "4.1.11",
    "vue-tsc": "3.3.11"
  },
  "//overrides": "CVE-2026-45822 remediation, and the only lever that reaches it. `shadcn-vue` needs `vue-metamorph`, which needs `postcss-styl`, which pins `stylus@^0.57.0` — and stylus below 0.59.0 parses CSS through `css@3` and the deprecated `source-map-resolve`, which is what drags in `decode-uri-component`. Pinning that package directly is not an option, because its only fixed release (0.5.0) is ESM-only and `source-map-resolve` requires it from CJS. stylus 0.59.0 replaced `css` with `@adobe/css-tools`, so overriding stylus deletes the branch instead of patching its leaf. `postcss-styl` is at its latest and still pins ^0.57.0; drop this override when it widens the range. Nothing here runs the stylus parser — the project has no .styl files, and the only thing the build takes from shadcn-vue is `dist/tailwind.css`.",
  "overrides": {
    "stylus": "0.64.0"
  },
  "engines": {
    "node": ">=24.15.0"
  },
  "//version": "Mirrors `version` in the root gradle.properties, which is the single source of truth. Deliberately WITHOUT the -SNAPSHOT suffix (npm SemVer has no such convention), so the two files are intentionally not byte-identical. Kept in step by hand — see docs/LEGAL.md §4.6. The version the site displays comes from GET /meta, never from here."
}
